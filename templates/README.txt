Place your illustration template image here as `template.png`.

- The backend expects `./templates/template.png` relative to the root of the repo.
- Prefer a PNG with a transparent area where the face should be placed, or note the placeholder coordinates to adjust in `backend/app/utils.py`.
